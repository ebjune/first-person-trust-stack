# Docker Compose (placeholder)

Future: FPS services (`apps/web`, `apps/validator`, `services/*`) plus optional VTI containers for local integration testing.

Phase 0 VTI deploy smoke was skipped — adapters target live `*.fpndtg.com` endpoints by default.
