# Governance Control Plane API

Structured governance rules for VTC and VTN scopes. Compiles to Rego for VTI PEP sync (Path A), native evaluation later.

Planned host: `governance.fpndtg.com`

Resources: `GovernanceFramework`, `RoleDefinition`, `MembershipPolicy`, `WitnessPolicy`, `RecognitionPolicy`, `TrustAnchor`, `CeremonyProfile`.
